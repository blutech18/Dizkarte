# Requirement → Implementation Traceability

Maps each requirement (see `.kiro/specs/dizkarte-platform/requirements.md`) to
its implementation in this pass. "Frontend" items are owned by the next pass.

| Req | Summary                                  | Implementation (this pass)                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| --- | ---------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| R1  | Auth & session                           | `schemas/auth.ts`; capability/actor model in `roles.ts`; RLS `to authenticated`; config rejects embedded secrets                                                                                                                                                                                                                                                                                                                                            |
| R2  | Identity verification                    | `verification_cases/documents/events` tables; `decide_verification` RPC; `submit_verification` schema; private `id-documents` bucket; manual-review copy (no KYC claim)                                                                                                                                                                                                                                                                                     |
| R3  | Tasker onboarding/governance             | `tasker_applications/profiles`, `service_areas`, `portfolio_items`, `payout_methods` (token boundary + raw-card check); `decide_tasker_application` schema; `public_tasker_profiles` excludes private data                                                                                                                                                                                                                                                  |
| R4  | Private-safe task posting                | Separated `task_public_locations`/`task_private_locations`; `public_task_feed` view; `toPublicTaskFeedItem` + `assertNoPrivateTaskFields`; owner-only RLS; `publish_task` RPC                                                                                                                                                                                                                                                                               |
| R5  | Discovery & nearby                       | `search_open_tasks` bounded/indexed function; `taskSearchSchema` bounds; feed/map share the OPEN predicate; indexes on city/geo/published                                                                                                                                                                                                                                                                                                                   |
| R6  | Questions, offers, single selection      | `task_questions`, `offers` (unique per tasker), `submit_offer` eligibility RPC; `select_offer` transactional + idempotent; `uq_booking_active_per_task`                                                                                                                                                                                                                                                                                                     |
| R7  | Payment/ledger/release/refund/withdrawal | `payment_intents`, `provider_events`, immutable balanced ledger, `process_payment_event`, `confirm_completion_and_release`, `request_withdrawal`; fee `bps` default 0; PHP centavos                                                                                                                                                                                                                                                                         |
| R8  | Paid-booking messaging                   | `conversations`/`participants`/`messages`/`message_media`; conversation created only on confirmed payment; participant-only RLS; private `chat-media` bucket; **assignment-scoped Admin access (0013): only participants and the Admin explicitly assigned to a report on the message or a dispute on the booking; storage bound to the real `message_media` row**                                                                                          |
| R9  | Completion & disputes                    | `request_completion`/`start_booking`/`confirm_completion_and_release`/`open_dispute`; booking/task state maps; `booking_events` history; no auto-release                                                                                                                                                                                                                                                                                                    |
| R10 | Reviews & reputation                     | `reviews` (one per reviewer/booking), blind `HIDDEN`→`REVEALED`; `submit_review` server-controlled aggregates                                                                                                                                                                                                                                                                                                                                               |
| R11 | Notifications                            | `notifications`/`notification_preferences`; created after commit inside RPCs; synthetic push labeled; prod push fails closed                                                                                                                                                                                                                                                                                                                                |
| R12 | Support/reports/Admin ops                | `reports`/`disputes`/`support_tickets`/`ticket_messages`/`evidence`; `moderation_actions`+`audit_logs` (append-only); capability-scoped RLS; **assignment-scoped narrative/evidence access + capability-only queue-metadata views (0013): `admin_report_queue`/`admin_dispute_queue`/`admin_ticket_queue`/`admin_verification_queue` expose non-narrative metadata; base rows/files require explicit assignment; active-account checks on all Admin gates** |
| R13 | Tasker Dashboard                         | `derive_user_balances` ledger-derived balances; `public_tasker_profiles`; (UI in next pass)                                                                                                                                                                                                                                                                                                                                                                 |
| R14 | Quality/accessibility/brand/ops          | 39-key theme contract + tests; CI (format/lint/typecheck/test/build + secret-safe); pagination bounds + indexes; runbooks in `docs/operations`                                                                                                                                                                                                                                                                                                              |

## Core invariants → enforcement

| Invariant                          | Where enforced                                                                                                    |
| ---------------------------------- | ----------------------------------------------------------------------------------------------------------------- |
| 1 Auth ≠ authorization             | RLS policies + `SECURITY DEFINER` re-checks                                                                       |
| 2 Gate exact/contact/chat          | table separation, RLS, `isCommunicationUnlocked`, conversation created on confirm; assignment-scoped Admin (0013) |
| 3 One active booking               | `uq_booking_active_per_task` + locked `select_offer`                                                              |
| 4 No self-confirm / tasker release | actor gates + `confirm_completion_and_release` client-only                                                        |
| 5 Derived balances                 | ledger + `derive_user_balances`; no mutable balance column                                                        |
| 6 Idempotency                      | unique keys + `provider_events` replay protection                                                                 |
| 7 Server-controlled + audited      | RPCs + `moderation_actions`/`audit_logs`; provider-authoritative refund/payout finalization (0013)                |
| 8 Short-lived private storage      | private buckets + `MediaSigner` TTL; row-bound object policies + safe path parsing (0013)                         |
| 9 No synthetic in prod             | config guard + `assertSyntheticAllowed` + edge fail-closed                                                        |
| 10 No real data in tests/logs      | synthetic fixtures; structured secret-free logging guidance                                                       |
